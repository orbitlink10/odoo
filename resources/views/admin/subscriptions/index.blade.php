<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">Super Admin: Subscriptions</h2></x-slot>
    <div class="py-6 max-w-7xl mx-auto sm:px-6 lg:px-8"><div class="bg-white shadow sm:rounded-lg overflow-hidden"><table class="min-w-full text-sm"><thead class="bg-gray-50"><tr><th class="p-3 text-left">Tenant</th><th class="p-3 text-left">Status</th><th class="p-3 text-left">Modules</th><th class="p-3 text-left">Action</th></tr></thead><tbody>@foreach($subscriptions as $subscription)<tr class="border-t"><td class="p-3">{{ $subscription->tenant?->name }}</td><td class="p-3">{{ $subscription->status }}</td><td class="p-3">{{ $subscription->modules->pluck('name')->join(', ') }}</td><td class="p-3"><form method="POST" action="{{ route('admin.subscriptions.status', $subscription) }}" class="flex gap-2">@csrf @method('PATCH')<select name="status" class="border rounded p-1"><option value="trialing">trialing</option><option value="active">active</option><option value="past_due">past_due</option><option value="canceled">canceled</option></select><button class="bg-blue-600 text-white px-2 rounded">Update</button></form></td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $subscriptions->links() }}</div></div>
</x-app-layout>
