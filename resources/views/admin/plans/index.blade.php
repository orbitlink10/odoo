<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">Super Admin: Plans</h2></x-slot>
    <div class="py-6 max-w-6xl mx-auto sm:px-6 lg:px-8 space-y-4"><div class="bg-white p-6 shadow sm:rounded-lg"><form method="POST" action="{{ route('admin.plans.store') }}" class="space-y-3">@csrf<div class="grid md:grid-cols-3 gap-3"><input name="name" placeholder="Plan Name" class="border rounded p-2" required><input name="slug" placeholder="Slug" class="border rounded p-2" required><input name="monthly_price" type="number" step="0.01" placeholder="Monthly Price" class="border rounded p-2" required><input name="trial_days" type="number" value="14" class="border rounded p-2" required><input name="description" placeholder="Description" class="border rounded p-2 md:col-span-2"></div><div class="grid md:grid-cols-2 gap-2">@foreach($modules as $module)<label class="border rounded p-2"><input type="checkbox" name="modules[]" value="{{ $module->id }}"> {{ $module->name }}</label>@endforeach</div><button class="bg-blue-600 text-white px-4 py-2 rounded">Create Plan</button></form></div><div class="bg-white p-6 shadow sm:rounded-lg"><ul class="space-y-2">@foreach($plans as $plan)<li>{{ $plan->name }} - KES {{ number_format($plan->monthly_price,2) }} ({{ $plan->modules->pluck('name')->join(', ') }})</li>@endforeach</ul>{{ $plans->links() }}</div></div>
</x-app-layout>
