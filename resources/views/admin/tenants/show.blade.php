<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">Tenant: {{ $tenant->name }}</h2></x-slot>
    <div class="py-6 max-w-6xl mx-auto sm:px-6 lg:px-8 space-y-4"><div class="bg-white p-6 shadow sm:rounded-lg"><p>Country: {{ $tenant->country }}</p><p>Currency: {{ $tenant->currency }}</p><p>Timezone: {{ $tenant->timezone }}</p><form method="POST" action="{{ route('admin.tenants.status', $tenant) }}" class="mt-4">@csrf @method('PATCH')<label class="flex items-center gap-2"><input type="checkbox" name="is_active" value="1" @checked($tenant->is_active)> Active</label><button class="mt-3 px-4 py-2 bg-blue-600 text-white rounded">Update Status</button></form></div><div class="bg-white p-6 shadow sm:rounded-lg"><h3 class="font-semibold mb-2">Users</h3><ul>@foreach($tenant->users as $user)<li>{{ $user->name }} ({{ $user->email }})</li>@endforeach</ul></div><div class="bg-white p-6 shadow sm:rounded-lg"><h3 class="font-semibold mb-2">Subscriptions</h3><ul>@foreach($tenant->subscriptions as $subscription)<li>{{ $subscription->status }} - {{ $subscription->modules->pluck('name')->join(', ') }}</li>@endforeach</ul></div></div>
</x-app-layout>
